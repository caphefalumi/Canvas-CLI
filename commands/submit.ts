// Temporary stub - full TypeScript migration pending for submit command
import type { Command } from 'commander';

export function submitAssignment(_command: Command): void {
  console.log('Submit command not yet migrated to TypeScript. Please use the JavaScript version.');
  process.exit(1);
}
